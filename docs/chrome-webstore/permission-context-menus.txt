The extension needs the "contextMenus" permission to offer “Open in new tab (allow duplicate)” when the user right-clicks a hyperlink on a page. The menu uses Chrome’s built-in link context, which supplies the link URL without injecting scripts into the page or requesting broad host access. That option opens the URL in a new tab without applying duplicate prevention for that tab. The menu is registered once in the background; no page content is read or stored.
